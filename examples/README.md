# ANA Examples

This directory contains example scripts demonstrating how to use the ANA library.

- `text_generation.py`: Basic text generation using `ANAModel`.
- `rl_agent.py`: Reinforcement Learning agent using `ANARLAgent`.
- `train_text_generation.py`: Complete training loop example using `Trainer` with checkpointing.
